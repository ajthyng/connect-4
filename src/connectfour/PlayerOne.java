package connectfour;

public class PlayerOne extends Player {

	public void requestExit() {
		//Implement a method to exit the game, not sure if it should be 
		//similar to Player.forfeit() or if it should be a separate type of exit with no winning condition 
	}
}
